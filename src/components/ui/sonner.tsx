'use client'

// Quanby Case Management Platform – Sonner Toast Wrapper
// Re-exports Toaster from sonner with platform-appropriate defaults.
// The Toaster is already mounted in src/app/layout.tsx.
// Import `toast` from 'sonner' directly in any component to trigger toasts.

export { Toaster } from 'sonner'
export { toast } from 'sonner'
